setInterval(() => { 
    const cleanupFolders = ['tmp', 'temp']; // Removed 'baileys'
    cleanupFolders.forEach(folder => {
        // Logic to delete folder
    });
}, 120000); // 120000 ms = 2 minutes
